// Placeholder (skip) para futura verificação de divergência entre soma das promissórias e total do pedido.
// Ativar quando usePedidoPromissorias expuser sumPromissorias e/ou mismatch flag.
describe('usePedidoPromissorias mismatch (placeholder)', () => {
  it('placeholder até expor sumPromissorias/mismatch (sem asserções de lógica)', () => {
    // Quando a lógica for implementada, substituir por testes reais.
    console.warn('[placeholder] mismatch promissorias ainda não implementado');
    expect(true).toBe(true);
  });
});
