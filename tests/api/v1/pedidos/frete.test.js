/**
 * @jest-environment node
 */
// Teste removido: modelo de frete por item (frete_unitario) foi descontinuado.
// Arquivo mantido vazio apenas para preservar histórico no controle de versão.
describe("Legacy frete_unitario (deprecated)", () => {
  test("placeholder - modelo substituído por frete_total agregado", () => {
    expect(true).toBe(true);
  });
});
