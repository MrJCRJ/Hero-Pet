/** @jest-environment node */
// TESTE DEPRECATED: endpoint /api/v1/pedidos/migrate_fifo_all removido.
// Mantido apenas como placeholder para preservar histórico de commits.
describe("(deprecated) POST /api/v1/pedidos/migrate_fifo_all", () => {
  test("placeholder - endpoint removido", () => {
    expect(true).toBe(true);
  });
});
